def palindromo(pal):
    if isinstance(pal)
        return pal_aux(pal)
def pal_aux(pal)
    if pal==[0]
        return pal_aux(pal)
    elif [pal[0]]==[pal[-1]]
        return "true"
    elif [pal[1:]]==[pal[:-1]]
        return "True"
    else:
        return "False"

def promedio(numero,divisor)
    if isinstance(numero,int)
        return promedio_aux(numero,divisor)
def promedio_aux(numero,divisor)
    if numero==0
        return promedio_aux(numero,divisor)
    elif numero>0
        return promedio_aux(0+numero%10)/divisor


